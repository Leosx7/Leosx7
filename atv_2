import {question} from 'readline-sync'
console.log('... Conversor de horas para minutos ....')

// Entrada
var horas = Number(question('Quantas horas:'))

// Processamento
var minutos = horas * 60

// Saída
console.log(`> ${horas}horas equivale a ${minutos}min`)
